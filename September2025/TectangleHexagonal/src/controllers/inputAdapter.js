// InputAdapter: wraps MediaPipe Tasks-Vision frame stream into normalized records.\n// TODO: implement initialize({ modelPath, runningMode }) and nextFrame(video) returning { t, label, scoreOpen, scoreFist, landmarks, fps }.\nexport const STATES = Object.freeze({\n  IDLE: 'IDLE',\n});\n\nexport async function initialize(/* options */) {\n  throw new Error('inputAdapter.initialize not implemented');\n}\n\nexport async function nextFrame(/* video */) {\n  throw new Error('inputAdapter.nextFrame not implemented');\n}\n
